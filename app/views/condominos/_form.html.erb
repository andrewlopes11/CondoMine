<%= form_with(model: condomino) do |form| %>
  <% if condomino.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(condomino.errors.count, "error") %> prohibited this condomino from being saved:</h2>

      <ul>
        <% condomino.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :nome, style: "display: block" %>
    <%= form.text_field :nome %>
  </div>

  <div>
    <%= form.label :cpf, style: "display: block" %>
    <%= form.text_field :cpf %>
  </div>

  <div>
    <%= form.label :contato, style: "display: block" %>
    <%= form.text_field :contato %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
